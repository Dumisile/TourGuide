<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dumisile.tourguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Johannesburg" />
        android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainGuide" />
        android:parentActivityName=".MainActivity"/>
        <activity android:name=".restaurants" />
        <activity android:name=".Drosss"
            android:parentActivityName=".restaurants"/>
        <activity android:name=".mashhh"
            android:parentActivityName=".restaurants"/>
        <activity android:name=".piattoooo"
            android:parentActivityName=".restaurants"/>
        <activity android:name=".malls" />
        <activity android:name=".zoo" />
        <activity android:name=".bar" />
        <activity android:name=".park" />
        <activity android:name=".spa"></activity>
    </application>

</manifest>